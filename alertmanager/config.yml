route:
  receiver: 'ceph-dashboard'
  group_wait: 10s
  group_interval: 10s
  repeat_interval: 10s

receivers:
  - name: 'ceph-dashboard'
    webhook_configs:
    - url: 'http://localhost:4200/api/prometheus_receiver'
